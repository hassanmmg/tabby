"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _fs = await jitiImport("fs");
var _path = await jitiImport("path");

// https://nuxt.com/docs/api/configuration/nuxt-config
var _default = exports.default = defineNuxtConfig({
  hooks: {
    'prepare:types': () => {
      // Ensure TypeScript config files exist for build
      const nuxtDir = (0, _path.join)(process.cwd(), '.nuxt');
      if ((0, _fs.existsSync)(nuxtDir)) {
        const tsconfigFiles = ['tsconfig.app.json', 'tsconfig.shared.json', 'tsconfig.node.json'];
        for (const file of tsconfigFiles) {
          const filePath = (0, _path.join)(nuxtDir, file);
          if (!(0, _fs.existsSync)(filePath)) {
            (0, _fs.writeFileSync)(filePath, JSON.stringify({ extends: './tsconfig.json' }, null, 2));
          }
        }
      }
    }
  },
  compatibilityDate: '2024-07-15',
  devtools: { enabled: false },
  experimental: {
    appManifest: false,
    typescriptBundlerResolution: false
  },
  typescript: {
    strict: false,
    shim: false
  },
  nitro: {
    experimental: {
      wasm: false
    },
    esbuild: {
      options: {
        target: 'esnext'
      }
    },
    cors: {
      origin: '*',
      credentials: true
    }
  },
  vite: {
    server: {
      hmr: {
        protocol: 'ws',
        host: 'localhost',
        port: 24678
      }
    }
  },
  modules: [
  '@nuxtjs/tailwindcss',
  '@pinia/nuxt',
  '@nuxt/image',
  '@nuxtjs/supabase'],

  css: ['~/assets/css/main.css'],
  app: {
    head: {
      title: 'Teknopuri Shop - Industrial Parts & Equipment',
      meta: [
      { charset: 'utf-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' },
      { name: 'description', content: 'Quality cooling parts, seats and accessories for forklifts and industrial equipment' }],

      link: []
    }
  },
  runtimeConfig: {
    // Private keys (server-side only)
    chipBrandId: process.env.CHIP_BRAND_ID || '',
    chipApiKey: process.env.CHIP_API_KEY || '',
    chipSandbox: process.env.CHIP_SANDBOX === 'true',
    // Public keys (available client-side)
    public: {
      apiBase: '/api',
      supabase: {
        url: process.env.SUPABASE_URL,
        key: process.env.SUPABASE_KEY
      }
    }
  },
  supabase: {
    redirect: false
  },
  image: {
    dir: 'public'
  }
}); /* v9-e95795ef7eb59f59 */
